# Recommendations

1. Add real section state: route or hash for Home / Works / Booking / Profile.
2. Update active nav semantically: `aria-current`, page title, route/hash.
3. Ship real fonts instead of relying on local OS fallbacks.
4. Redesign bottom nav as a stable mobile control: fixed hit areas, correct icons, visible focus.
5. Move grain below UI text/icons.
6. Normalize scroll model and stop hiding all scroll affordance blindly.
7. Split typography roles instead of reusing one heading formula everywhere.
8. Fix heading hierarchy per screen.

